import { ScienceFilterPipe } from './science-filter.pipe';

describe('ScienceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScienceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
